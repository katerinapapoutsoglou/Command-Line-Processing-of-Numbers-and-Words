# Command-Line-Processing-of-Numbers-and-Words
The program processes command-line arguments for integers and words. It stores numbers and rearranges them based on a condition relative to the first element. It also takes a character as input and removes it from all words. Finally, it prints the modified numbers and updated words along with how many characters were removed.
